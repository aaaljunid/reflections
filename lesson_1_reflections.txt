what is git and gitHub?

- git is a versiton control system it help the developer to save his work and backe it at any time.
- gitHub is a web application related to git systems which like you can share your works and you can works 
  with others in the same projects.
-There are so meny system like git but git have access offline.
- we can compare between to files to find the bugs manually by using terminal and write code FC then NewFile.js 
  then space then OldFile.
- the component of Git: 1- commit. 2- diff.
-Reflect: Multi-File Commits

-Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately?
because gi is alow to use multiple diffrent files off line.

-How can you use the commands git log and git diff to view the history of files?
it was easy but  i do not know how i get more commits by going down of page.

-How might using version control make you more confident to make changes that could break something?
by using commnd git checkout and then find the diffrant of code by u

-Now that you have your workspace set up, what do you want to try using Git for?
Node.js command prompt

Git command review
Compare two commits, printing each line that is present in one commit but not the other.

git diff will do this. It takes two arguments - the two commit ids to compare. 


Make a copy of an entire Git repository, including the history, onto your own computer.

git clone will do this. It takes one argument - the url of the repository to copy. 


Temporarily reset all files in a directory to their state at the time of a specific commit.

git checkout will do this. It takes one argument - the commit ID to restore. 


Show the commits made in this repository, starting with the most recent.

git log will do this. It doesn't take any arguments.



*Git commands:
git init
git status
git log
git diff
git checkout
git config --global color.ui auto










